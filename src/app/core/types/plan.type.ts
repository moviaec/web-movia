/**
 * Who a plan is for, with the values `api-movia` stores in `plans.audience`.
 *
 * They travel as they are written here: `b2c` is the individual plans and
 * `empresa` the corporate one, which the plans page paints in its own block.
 */
export type PlanAudience = 'b2c' | 'empresa';
