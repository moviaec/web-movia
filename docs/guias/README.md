# docs/guias/

Instrucciones para ejecutar algo: cómo arrancar el proyecto, cómo desplegar, runbooks de incidencias. Se mantienen al día, porque alguien las va a seguir tal cual.

No va aquí el registro histórico de una tarea (eso es `../cambios/`) ni la portada del repositorio (`README.md` de la raíz).
